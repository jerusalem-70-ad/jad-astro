---
import NavLink from "@/components/nav-link.astro";
import { Icon } from "astro-icon/components";
import { Image } from "astro:assets";
interface Props {
	title: string;
	summary?: string;
	url?: string | null;
	icon?: string;
	image: any;
}

const { title, summary, url, icon, image } = Astro.props;
---

<article>
	<!-- Mobile view -->
	<!-- <div
		class="md:hidden my-2 border-t-[5px] border-brand-800 bg-white shadow-md rounded-md rounded-t-none"
	>		
		<div class="grid gap-y-4 px-4 p-8 xs:px-8">
			<h2 class="text-brand-800 text-2xl font-semibold">
				<NavLink href={url} class="no-underline">
					{title}
				</NavLink>
			</h2>

			<p class="text-brand-600 font-medium italic">
				{summary}
			</p>

			<NavLink href={url} class="no-underline py-2 px-4 bg-brand-800 text-white rounded-md w-fit">
				Read more
			</NavLink>

			<slot />
		</div>
	</div> -->

	<!-- Desktop view -->
	<div >
		<NavLink href={url} class="no-underline">
			<div class="flex h-16 overflow-hidden rounded-md shadow-md text-center bg-brand-800/90 text-brand-50/80 p-2 transition-all duration-300 hover:bg-brand-900/95 justify-items-start gap-1"
				>
				<Icon class="w-1/4 self-center inline-flex " aria-hidden="true" name={icon} />
				<h2 class="w-3/4 self-center text-sm font-semibold">
					<span class="no-underline">{title}</span>
				</h2>
					
			</div>
		</NavLink>
	</div>
</article>
