---
import NavLink from "@/components/nav-link.astro";
import { Icon } from "astro-icon/components";
import { Image } from "astro:assets";
interface Props {
	title: string;
	summary?: string;
	url?: string | null;
	icon?: string;
	image: any;
}

const { title, summary, url, icon, image } = Astro.props;
---

<article>
	<!-- smaller screen -->
	<div
		class="md:hidden my-2 border-t-[5px] border-brand-800 bg-white shadow-md rounded-md rounded-t-none h-full"
	>
		<!-- Image -->
		<Image src={image} alt="" class="hidden sm:block h-46 w-full object-cover" />

		<!-- Text content -->
		<div class="flex flex-col gap-y-4 px-6 py-3">
			<h2 class="text-brand-800 text-xl font-semibold capitalize">
				<NavLink href={url} class="no-underline">
					{title}
				</NavLink>
			</h2>

			<p class="text-brand-600 font-medium italic">
				{summary}
			</p>

			<NavLink href={url} class="self-end no-underline py-2 px-4 bg-brand-800 text-white rounded-md w-fit ">
				Read more
			</NavLink>

			<slot />
		</div>
	</div>

	<!-- Desktop view -->
	<div class="hidden md:block">
		<NavLink href={url} class="no-underline">
			<article class="relative h-60 group overflow-hidden rounded-md shadow-md text-center">
				<!-- Background image  -->
				<div class="absolute inset-0">
					<Image src={image} alt="" class="w-full h-full object-cover" />
					<div class="absolute inset-0 bg-black opacity-25"></div>
				</div>
				<!-- Text content of the card -->
				<div
					class="relative z-10 h-full flex flex-col transition-transform duration-300 transform translate-y-[calc(50%-2rem)] group-hover:translate-y-0 hover:backdrop-blur-xs hover:bg-black/40"
				>
					<header class="p-4 text-white text-shadow-lg/30">
						<Icon class="inline-flex w-6 h-6" aria-hidden="true" name={icon} />
						<h2 class="text-2xl font-semibold capitalize">
							<span class="no-underline">{title}</span>
						</h2>
					</header>
					<!-- Hidden text appears on hover -->
					<div
						class="opacity-0 group-hover:opacity-100 transition-opacity duration-300 space-y-4 p-5"
					>
						<p class="text-brand-50 font-medium text-lg italic">
							{summary}
						</p>
						<slot />
					</div>
				</div>
			</article>
		</NavLink>
	</div>
</article>
