import type { Graph } from "@/types/index";

export class ChronologicalGraph {
  constructor(container: HTMLElement, graphData: Graph) {}

  render() {}
  updateFilter(ids: Set<string>) {}
  destroy() {}
}
